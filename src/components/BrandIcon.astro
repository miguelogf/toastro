---
import { BRANDS } from "../utils/brands";

interface Props {
  name: keyof typeof BRANDS;
}

const { name } = Astro.props as Props;
const icon = BRANDS[name];
---

{icon ? <Fragment set:html={icon} /> : <span>Icon not found</span>}
