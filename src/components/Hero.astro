---
import Badge from "../components/Badge.astro";
import ToastContainer from "../components/ToastContainer.astro";
import logoLight from "../assets/logo-light.svg";
import logoDark from "../assets/logo-dark.svg";
import Button from "../components/Button.astro";

// Nota: El import de ICONS dentro del frontmatter sirve si pasas datos a componentes,
// pero para el <script> del cliente lo importaremos dentro del script.
---

<section class="hero">
  <div class="hero-content">
    <Badge
      label="Coming Soon"
      variant="gradient"
      color="var(--accent-color)"
      gradientColors={["#f97316", "#f31b75"]}
    />

    <div class="logo-container" aria-hidden="true">
      <div class="logo-wrapper">
        <img src={logoDark.src} alt="" class="logo-dark" />
        <img src={logoLight.src} alt="" class="logo-light" />
      </div>
      <span class="logo-text"
        >toast<span class="text-gradient-toastro">ro</span></span
      >
    </div>

    <h1 class="hero-title">
      A beautiful and minimal<br />
      <span class="text-gradient-orange">toast</span> for
      <span class="text-gradient-pink">Astro.</span>
    </h1>

    <div class="buttons">
      <Button id="btn-toast">Toast me! ✨</Button>
      <Button
        href="https://github.com/miguelogf/toastro"
        variant="outline"
        newTab
      >
        Documentation
      </Button>
    </div>
  </div>
</section>

<ToastContainer />

<script>
  import { ICONS } from "../utils/icons";

  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("btn-toast");

    const toastOptions = [
      {
        message: "Successfully deployed to Vercel",
        type: "success",
        variant: "solid",
        icon: ICONS.success,
      },
      {
        message: "Warning: Low disk space",
        type: "warning",
        variant: "soft",
        icon: ICONS.warning,
      },
      {
        message: "Thanks for the support!",
        type: "support",
        variant: "outline",
        icon: ICONS.vibrant,
      },
      {
        message: "New update available",
        type: "loading",
        variant: "solid",
        icon: ICONS.neutral,
      },
      {
        message: "🍞✨ A minimal toast for Astro",
        variant: "gradient",
        gradientColors: ["#6366f1", "#f31b75"],
      },
    ];

    btn?.addEventListener("click", () => {
      const randomConfig =
        toastOptions[Math.floor(Math.random() * toastOptions.length)];
      window.showToast?.(randomConfig as any);
    });
  });
</script>

<style>
  .hero {
    min-height: 100vh;
    display: grid;
    place-items: center;
    padding: 0 1rem;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    width: 100%;
    max-width: 800px; /* Evita que el texto se vea muy largo en monitores grandes */
    margin: 0 auto;
    gap: 1.5rem;
  }

  /* LOGO */
  .logo-container {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.5rem;
    margin-top: -0.75rem;
  }

  .logo-wrapper {
    width: 2.5rem;
    aspect-ratio: 1 / 1;
  }

  .logo-text {
    font-size: 3rem;
    font-weight: 800;
    letter-spacing: -1px;
  }

  .logo-dark {
    display: none;
  }
  html.dark .logo-dark {
    display: block;
  }
  html.dark .logo-light {
    display: none;
  }

  /* TÍTULO PRINCIPAL (H1) */
  .hero-title {
    font-size: clamp(2rem, 8vw, 3.5rem); /* Tamaño fluido según pantalla */
    font-weight: 200;
    margin: 0;
    line-height: 1.1;
    text-wrap: nowrap;
  }

  /* GRADIENTES */
  .text-gradient-toastro {
    background: linear-gradient(to right, #f97316, #ff000e, #bf0055);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  .text-gradient-orange {
    background: linear-gradient(to right, #f97316, #fbbf24);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: 700;
  }
  .text-gradient-pink {
    background: linear-gradient(to right, #ff000e, #bf0055);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: 700;
  }

  .buttons {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
    justify-content: center; /* Asegura que estén centrados en desktop */
    width: 100%;
  }
  /* --- RESPONSIVE ADJUSTMENTS --- */

  @media (max-width: 640px) {
    .hero-title {
      text-wrap: balance;
    }

    .buttons {
      flex-direction: column;
      align-items: stretch; /*Estira los botones al mismo ancho*/
      gap: 0.8rem;
      padding: 0 1rem; /* Margen interno para que no toquen los bordes */
    }

    .logo-text {
      font-size: 2.5rem; /* Un poco más pequeño en móvil */
    }
  }
</style>
