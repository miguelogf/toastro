---
import logo from "../assets/logo.svg";
import SocialLink from "./SocialLink.astro";
import ThemeToggle from "./ThemeToggle.astro";

const socials = [
  { name: "github", href: "https://github.com/miguelogf" },
  { name: "linkedin", href: "https://www.linkedin.com/in/miguelogf/" },
  { name: "x", href: "https://x.com/_miguelogf" },
] as const;
---

<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-left">
      <a href="/"><img src={logo.src} alt="Logo" class="logo" /></a>
    </div>

    <div class="navbar-menu" id="navMenu">
      <div class="nav-links">
        <a href="/about" class="nav-link">About</a>
        <a href="/docs" class="nav-link">Docs</a>
        <a href="/changelog" class="nav-link">Changelog</a>
      </div>

      <div class="mobile-socials">
        {
          socials.map((link) => (
            <SocialLink name={link.name} href={link.href} />
          ))
        }
      </div>
    </div>

    <div class="navbar-right">
      <div class="desktop-socials">
        {
          socials.map((link) => (
            <SocialLink name={link.name} href={link.href} />
          ))
        }
      </div>
      <div class="spacer"></div>
      <ThemeToggle />
      <button class="menu-toggle" id="menuToggle" aria-label="Menu">
        <span class="line top"></span>
        <span class="line bottom"></span>
      </button>
    </div>
  </div>
</nav>

<script>
  const menuToggle = document.getElementById("menuToggle");
  const navMenu = document.getElementById("navMenu");
  const body = document.body;

  menuToggle?.addEventListener("click", () => {
    const isOpened = menuToggle.classList.toggle("is-active");
    navMenu?.classList.toggle("is-visible");

    // Evita el scroll cuando el menú está abierto
    body.style.overflow = isOpened ? "hidden" : "";
  });
</script>

<style>
  .navbar {
    position: sticky;
    top: 0;
    z-index: 100;
    /* Sin backdrop-filter aquí: evita crear un stacking context que
       atraparía al .navbar-menu fixed e impediría su propio blur */
    background: transparent;
    transition: color 0.75s ease;
    border-bottom: 1px solid
      color-mix(in srgb, var(--text-color) 5%, transparent);
    padding: 0.75rem 1.5rem;
  }

  /* El blur del navbar va en un pseudo-elemento para no crear stacking context */
  .navbar::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background: color-mix(in srgb, var(--bg-color) 75%, transparent);
    backdrop-filter: blur(1rem);
    -webkit-backdrop-filter: blur(1rem);
    transition: background-color 0.75s ease;
  }

  .navbar-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
  }

  .logo {
    height: 1.5rem;
    width: auto;
  }

  /* Desktop Menu */
  .nav-links {
    display: flex;
    gap: 2rem;
  }
  .nav-link {
    text-decoration: none;
    color: var(--text-color);
    font-weight: 400;
    font-size: 0.9rem;
    opacity: 0.8;
    transition: opacity 0.2s;
  }
  .nav-link:hover {
    opacity: 1;
  }

  .navbar-right {
    display: flex;
    flex-direction: row !important;
    align-items: center;
    gap: 1rem;
  }
  .desktop-socials {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 0.75rem;
  }
  .spacer {
    width: 1px;
    height: 1.5rem;
    background: var(--text-color);
    opacity: 0.2;
  }

  /* Hamburguesa: El arte de las 2 líneas */
  .menu-toggle {
    display: none;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 6px; /* Espacio entre las 2 líneas */
    width: 2rem;
    height: 2rem;
    background: none;
    border: 1px transparent;
    border-radius: 8px;
    cursor: pointer;
    z-index: 101;
    padding: 0;
  }

  .menu-toggle:hover {
    background: rgba(128, 128, 128, 0.2);
  }

  .line {
    width: 22px;
    height: 1.5px;
    background-color: var(--text-color);
    border-radius: 1px;
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Animación de la X sin deformación */
  .menu-toggle.is-active .line.top {
    /* 3.75px es el cálculo mágico: (6px gap + 1.5px height) / 2 */
    transform: translateY(3.75px) rotate(45deg);
  }

  .menu-toggle.is-active .line.bottom {
    transform: translateY(-3.75px) rotate(-45deg);
  }

  /* Responsive Logic */
  @media (min-width: 769px) {
    .navbar-menu {
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
    }
    .mobile-socials {
      display: none;
    }
  }

  @media (max-width: 768px) {
    .menu-toggle {
      display: flex;
    }
    .desktop-socials,
    .spacer {
      display: none;
    }

    .navbar-menu {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100dvh;
      background: color-mix(in srgb, var(--bg-color) 75%, transparent);
      backdrop-filter: blur(1.5rem);
      -webkit-backdrop-filter: blur(1.5rem);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      gap: 3rem;
      opacity: 0;
      visibility: hidden;
      transition:
        opacity 0.7s cubic-bezier(0.4, 0, 0.2, 1),
        visibility 0.5s;
      z-index: 100;
    }

    .navbar-menu.is-visible {
      opacity: 1;
      visibility: visible;
    }

    .nav-links {
      flex-direction: column;
      align-items: center;
      gap: 2.5rem;
    }

    .nav-link {
      font-size: 1.5rem;
      font-weight: 300;
      transform: translateY(15px);
      opacity: 0;
      transition:
        transform 0.4s cubic-bezier(0.22, 1, 0.36, 1),
        opacity 0.4s ease;
    }
    /* Cuando el menú se abre */
    .navbar-menu.is-visible .nav-link {
      transform: translateY(0);
      opacity: 0.7;
    }
    .navbar-menu.is-visible .nav-link:hover {
      opacity: 1;
    }

    /* Efecto escalonado (Stagger) */
    .nav-link:nth-child(1) {
      transition-delay: 0.1s;
    }
    .nav-link:nth-child(2) {
      transition-delay: 0.2s;
    }
    .nav-link:nth-child(3) {
      transition-delay: 0.3s;
    }

    .mobile-socials {
      display: flex;
      gap: 2rem;
      margin-top: 1rem;
    }
  }
</style>
